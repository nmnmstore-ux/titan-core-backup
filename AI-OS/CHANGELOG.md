# 📜 سجل التغييرات — {{PROJECT_NAME}}

> **⚠️ مصدر الحقيقة الوحيد (Single Source of Truth):** هذا الملف هو **المصدر الوحيد** لكل تغيير.
> لو المشروع فيه داشبورد يقرأ بيانات من ملفات TS/JSON (مثل `src/lib/changelog.ts`) → هذه الملفات **مرايا (mirrors)** تُحدث منه، **لا تُحرر يدويًا أبدًا**.

> **قاعدة إلزامية (من AI_MANDATE):** أي إضافة أو تغيير في الكود أو النظام يُسجَّل هنا **فورًا** مع نهاية كل مهمة.
> تنسيق: `[YYYY-MM-DD] ما تم — نوع التغيير — (اسم الكاتب/المراجع)`
> ترتيب: الأحدث في الأعلى.

## {{INSTALL_DATE}}

- **مصدر الحقيقة الوحيد** — نظام — CHANGELOG.md هو المصدر الوحيد للسجل، وEXECUTION_AGENDA.md هو المصدر الوحيد للخطة. أي ملفات بيانات داشبورد (TS/JSON) تُعامل كمرايا تُحدث من المصدر فقط.
- **بروتوكول البحث والابتكار** — نظام — RESEARCH_PROTOCOL.md: إلزامي لأي موديل بالبحث عن أحدث ما توصل إليه العالم قبل القرارات المهمة + ابتكار بفحص جدوى. مُربوط بالدستور وتسلسل الانطلاق.
- **توسيع نطاق البحث — الطبقة العميقة المشروعة** — نظام — مصادر عميقة قانونية أُضيفت (أوراق مدفوعة، براءات عالمية، قواعد ثغرات صناعية، تقارير سوق، بيانات ضخمة قانونية). الخط الأحمر الوحيد: أي مصدر يعرّض النظام القانوني/التشغيلي لمخاطرة عملية فعلية.
- **قرار المستخدم: البحث العميق L3 معيار وحيد** — نظام — أُلغي L1/L2/L3 الطبقي: كل قرار من الأصغر للأكبر يُبحث بحثًا عميقًا كاملًا موثقًا.
- **6 إضافات قوة للقالب** — نظام — RESEARCH_LOG.md (سجل أبحاث مفتوح = برهان البحث) + Audit Gate (8 بنود قبل "تم") + Fact Currency (معلومة >6 أشهر = قديمة) + Contradiction Check (أول كل جلسة) + Lessons Learned (دمج الدروس) + Versioning (رفع إصدار الدستور). ملفات جديدة: `RESEARCH_LOG.md`.
- **مثبّت تلقائي install.ps1** — إعداد — ملف واحد ينسخ القالب ويستبدل الاسم تلقائيًا: `powershell -File install.ps1` → يسأل مسار المشروع + الاسم → ينتهي. يتجاوز الملفات المخصصة الموجودة.
- **طبقة الابتكار والتشغيل الذاتي (AI_MANDATE §13)** — نظام — ابتكار العلاقات الربحية (خريطة نظام×نظام، معروفة ومبتكرة، بفحص جدوى) + رشاد التعاون الخارجي (بموافقة المستخدم) + التشغيل الذاتي الكامل Hands-off (أي موديل يستلم ويدير، المواكبة ببحث L3، تبديل الموديل = صفر خسارة). المستخدم يراقب من بعيد برجل على رجل.

---

## القالب المبدئي
```
[YYYY-MM-DD] ما تم — نوع التغيير (نظام/إصلاح/إعداد/توثيق/اختبارات) — (الكاتب)
```
